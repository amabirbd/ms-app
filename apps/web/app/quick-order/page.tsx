import { WorkspacePage } from "../_components/workspace-page";
export default function Page() {
  return (
    <WorkspacePage
      eyebrow="Procurement tools"
      title="Quick order"
      description="Add SKUs directly, upload a validated CSV, or start from a saved order template."
    />
  );
}
